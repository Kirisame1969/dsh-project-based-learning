# 统一行尾：仓库内文本文件一律 LF，避免跨平台 diff 噪音
* text=auto eol=lf

# 明确标记为二进制，避免被当成文本处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.zip binary

# Windows 脚本保持 CRLF（如将来补充 .ps1）
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
